<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LPSimulation"
        tools:targetApi="31">
        <activity
            android:windowSoftInputMode="adjustNothing"
            android:name=".activities.controller.Onboarding.OnBoardingVC"
            android:exported="true"
            android:theme="@style/FullScreen"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".activities.controller.Setup.SetupVC"
            android:theme="@style/Theme.LPSimulation.NoActionBar"
            android:exported="true">
        </activity>
        <activity
            android:configChanges="keyboardHidden|orientation|screenSize|colorMode"
            android:name=".activities.controller.Home.HomeVC"
            android:theme="@style/Theme.LPSimulation.NoActionBar"
            android:exported="true">
        </activity>
        <activity
            android:configChanges="keyboardHidden|orientation|screenSize|colorMode"
            android:name=".activities.controller.profile.ProfileVC"
            android:theme="@style/Theme.LPSimulation.NoActionBar"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.list.ShopListView"
            android:theme="@style/Theme.LPSimulation.NoActionBar"
            android:exported="true">
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

    <permission android:name="Manifest.permission.CAMERA"
        tools:ignore="ManifestOrder" />
    <uses-feature android:name="android.hardware.camera"
        android:required="true"
        tools:ignore="ManifestOrder" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
</manifest>