<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LPSimulation"
        tools:targetApi="31">
        <activity
            android:name=".activities.base.BaseTabbarViewController"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LPSimulation.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".activities.list.JobListViewController"
            android:theme="@style/Theme.LPSimulation.NoActionBar"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.list.SectionListViewController"
            android:theme="@style/Theme.LPSimulation.NoActionBar"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.list.AssetsListViewController"
            android:theme="@style/Theme.LPSimulation.NoActionBar"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.list.ShopListView"
            android:theme="@style/Theme.LPSimulation.NoActionBar"
            android:exported="true">
        </activity>
    </application>

    <permission android:name="Manifest.permission.CAMERA"
        tools:ignore="ManifestOrder" />
    <uses-feature android:name="android.hardware.camera"
        android:required="true"
        tools:ignore="ManifestOrder" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
</manifest>